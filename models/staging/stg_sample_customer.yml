version: 2

models:
  - name: stg_sample__customer
    description: ""
    columns:
      - name: c_mktsegment
        data_tests:
          - accepted_values:
              values: ["FURNITURE", "HOUSEHOLD", "BUILDING", "AUTOMOBILE"]
              config:
                severity: warn